# ========================================
# Web Application Configuration
# ========================================

# Environment (development, production)
NODE_ENV=development

# ========================================
# API Configuration
# ========================================

# API base URL - where the backend API is hosted
# Must include /api/v1 path prefix
# Local development: http://localhost:8000/api/v1
# Production: https://api.yourdomain.com/api/v1
NEXT_PUBLIC_SERVER_URL=http://localhost:8000/api/v1

# Frontend URL for redirects and CORS
# Used for OAuth callbacks and API responses
NEXT_PUBLIC_CLIENT_URL=http://localhost:3000

# ========================================
# Authentication (Better Auth)
# ========================================

# Must match the server's BETTER_AUTH_SECRET
# This is used by the client-side auth library
BETTER_AUTH_SECRET=your_random_secret_here

# Backend auth endpoint URL
# Must match the API's BETTER_AUTH_URL
BETTER_AUTH_URL=http://localhost:3000

# Cookie key for storing session tokens
# Must match the server configuration
BETTER_AUTH_TOKEN_KEY=better-auth.session_token

# ========================================
# OAuth Providers
# ========================================

# Google OAuth Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Twitter (X) OAuth Configuration
# Get credentials from: https://developer.twitter.com/en/portal/dashboard
TWITTER_CLIENT_ID=your_twitter_client_id
TWITTER_CLIENT_SECRET=your_twitter_client_secret

# Microsoft OAuth Configuration
# Get credentials from: https://portal.azure.com/
MICROSOFT_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret

# ========================================
# Database (PostgreSQL)
# ========================================

# PostgreSQL connection URL (used in Next.js API routes if needed)
# Format: postgresql://user:password@host:port/database
# Example for Neon (serverless Postgres):
# DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require&channel_binding=require
DATABASE_URL=postgresql://user:password@localhost:5432/styxflow

# ========================================
# Real-time Communication (VAPI)
# ========================================

# VAPI public key for voice/video calls
# Get from: https://dashboard.vapi.ai/keys
# This is public and safe to expose in the browser
NEXT_PUBLIC_VAPI_PUBLIC_KEY=your_vapi_public_key

# VAPI workflow ID for interview calls
# Get from: https://dashboard.vapi.ai/workflows
# This is public and safe to expose in the browser
NEXT_PUBLIC_VAPI_WORKFLOW_ID=your_vapi_workflow_id

# ========================================
# File Upload (Cloudinary)
# ========================================

# Cloudinary cloud name for image/file uploads
# Get from: https://cloudinary.com/console/settings/api-keys
# This is public and safe to expose in the browser
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name

# Cloudinary API key
# Get from: https://cloudinary.com/console/settings/api-keys
CLOUDINARY_API_KEY=your_cloudinary_api_key

# Cloudinary API secret (KEEP PRIVATE - never expose in browser)
# Get from: https://cloudinary.com/console/settings/api-keys
# Only use this in server-side API routes (getServerSideProps, API routes)
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ========================================
# Optional: Analytics & Monitoring
# ========================================

# Enable error tracking (Sentry, LogRocket, etc.)
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Enable analytics (Google Analytics, Mixpanel, etc.)
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id

# ========================================
# Optional: Feature Flags
# ========================================

# Enable experimental features
# NEXT_PUBLIC_ENABLE_EXPERIMENTAL_FEATURES=false

# Enable demo mode
# NEXT_PUBLIC_DEMO_MODE=false

# ========================================
# Build Configuration
# ========================================

# Next.js image optimization
# Set to 'unoptimized' if using static export
# NEXT_PUBLIC_IMAGE_OPTIMIZATION=true

# Disable telemetry (optional)
# NEXT_TELEMETRY_DISABLED=1
